{
  "scripts": {
    "generate:config": "node ./docs/.vuepress/generateConfig.js",
    "dev": "vuepress dev docs",
    "build": "vuepress build docs",
    "deploy": "sh deploy.sh"
  },
  "devDependencies": {
    "vuepress": "^1.2.0"
  }
}
